#include <stdio.h>
#include "lcd.h"
#include "drivers/mss_uart/mss_uart.h"

int main() {
	LCD_init();

	char string[] = "Hello world eat my ass \n i wanna eat shit";
	//MSS_UART_polled_tx( &g_mss_uart1, (uint8_t*)string, sizeof(string) );
	LCD_printString(string, sizeof(string));

	while(1) {

	}
}
